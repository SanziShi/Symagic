订单精确查询提交数据出现json解析错误 hj(fix)

qucik search url 错误 yf

购物车在商品数量减到零时无法删除商品 zsc

isbn正确性检测 hj (fix)

出版时间 hj(fix)

结算页面点击积分，然后取消积分后跳ERROR/nullPointerException wqm(fix/ 一部分没有更改，是前端问题）

结算页面点击积分，页面不允许提交 yf

系统安装有问题 hj(fix)


购物车详情中，调用update时，没有刷新页面，造成价格处没有刷新。对于 update 商品项的数量为0时，删除了，但没有刷新页面，导致页面上还有相应商品项存在的情况  yf

增加评论时，同一个会员对同一件商品评论第二次时，出现duplicate entry \zsc

order_submit时调用的接口是check_score yf

用户修改密码时要不要进行数字字母检测 wqm（变更需求）

购物车详情中，在商品的数量框中，修改数量，焦点没离开，点结算，这时没调用update接口，但数量输入框中数量更改，造成显示与session不一致  yf



后台修改订单页面中，修改数量时，积分小计没有对应的更改。gz

下订单时没有使用积分 ，但在订单显示时却使用了积分 。wqm

管理员在修改订单时没有更新总价 hj (fix)

在购物车URL非法请求，返回数据 zsc

下单时电话和手机可以为字符串 wqm(fix)

销售量统计时，销售下限应为大于等于结果而不是大于的结果 xx

销售量统计时，搜索结果有错 xx

销售量统计页面上，显示有问题，“该类别总销售额”换个说法 gz

订单修改商品名称不显示 gz

订单修改，后台逻辑处理不完善 hj (fix)

下单确定页面，后台逻辑处理不完善 wqm

会员管理下一页跳转到item_detail hj(fix)

首页新书速递和热销商品似乎置换。。。 yf

后台页面跳转js失效（ie更为严重） gz

后台订单修改的页面无法动态更新地址

